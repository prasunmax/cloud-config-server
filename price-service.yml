
hibernate:
  ogm:
    datastore:
      create_database: true
      host: mongo
      password: example
      port: 27017
      provider: mongodb
      username: root
    mongodb:
      authentication_database: admin
microservices:
  endpoints:
    endpoint:
      productCatalog:
        products: http://PRODUCT-CATALOG-SERVICE/api/products
