
spring:
  cloud:
    gateway:
      routes:
        - id: product-catalog-service
          uri: lb://product-catalog-service
          predicates:
            - Path=/api/products/**, /api/products
          filters:
            - name: CircuitBreaker
              args:
                name: product-catalog-service
                fallbackuri: forward:/productFallback,false
hystrix:
  command:
    fallback:
      isolation:
        thread:
          timeoutInMilliseconds: 5000   
          
management:
  security:
    enabled: false 
  endpoints:
    web:
      exposure:
        include: "*"
