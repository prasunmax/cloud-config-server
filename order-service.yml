hibernate:
  ogm:
    datastore:
      create_database: true
      host: mongo
      password: example
      port: 27017
      provider: mongodb
      username: root
    mongodb:
      authentication_database: admin
microservices:
  endpoints:
    endpoint:
      cart:
        cart: cart-service
      productCatalog:
        products: product-catalog-service
